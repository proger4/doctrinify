# Open Questions

- Should AI executor stay heuristic-only, or integrate a pluggable local LLM adapter?
- Which 10 sandbox models are mandatory for pre-rollout hotspot checks?
- Do we need strict schema for task JSON outputs with validation failures on mismatch?
