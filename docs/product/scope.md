# Scope

In scope:
- `tools:orm:generate` and `tools:orm:clean` for deterministic artifact lifecycle.
- `tools:ai:task-execute` for diagnostic task execution from workbook.
- `tools:report:build` for local HTML diagnostic reports.

Out of scope:
- automatic modification of generated artifacts by AI;
- embedding prompt logic into generator business logic.
