# Vision

`doctrinify` is a deterministic generator for converting legacy model metadata into ORM artifacts.

Deployment-safe target formula:
- generator is deterministic;
- AI is auxiliary;
- reports are separate;
- rollout decisions are made by a human operator.
