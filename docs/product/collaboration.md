# Collaboration

Rules for parallel work:
- keep sidecar boundary intact (`orm` vs `ai/report`);
- confirm claims via code/tests/command outputs;
- prefer small, reviewable changes;
- update docs in the same change set as behavior changes.
