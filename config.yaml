models_path: 'sandbox/models'

base_classes:
  - 'app\\models\\BaseModel'

doctrine_xml_path: 'sandbox/doctrine'

schema_path: 'tests/_data/mock/database/schema.sql'

blacklist:
  - 'app\\models\\Old*'
  - 'app\\models\\Test*'

model_scan_exclude_dirs:
  - 'vendor'
  - 'tests'
  - 'runtime'

flags:
  generate_doctrine_xml: true
  generate_php_accessors: true

tooling:
  doctrine_xml:
    root_attributes:
      xmlns: "http://doctrine-project.org/schemas/orm/doctrine-mapping"
      xmlns:xsi: "http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation: "http://doctrine-project.org/schemas/orm/doctrine-mapping https://doctrine-project.org/schemas/orm/doctrine-mapping.xsd"
    filename_pattern: "{class}.orm.xml"
  regeneration:
    naming: "doctrinify"
    add_generated_marker: true
    embed_diagnostics: true
